Sql.Database("{{ src.server }}", "{{ src.database }}"
{%- if src.options and (src.options.get('commandTimeout') or src.options.get('encryptConnection') or src.options.get('trustServerCertificate')) -%}
, [
{%- if src.options.get('commandTimeout') %}
  CommandTimeout = #duration(0, 0, {{ src.options.get('commandTimeout') }}, 0)
{%- if src.options.get('encryptConnection') or src.options.get('trustServerCertificate') %},{% endif %}
{% endif %}
{%- if src.options.get('encryptConnection') %}
  EncryptConnection = {{ src.options.get('encryptConnection') | lower }}
{%- if src.options.get('trustServerCertificate') %},{% endif %}
{% endif %}
{%- if src.options.get('trustServerCertificate') %}
  TrustServerCertificate = {{ src.options.get('trustServerCertificate') | lower }}
{% endif %}
]
{%- endif -%}
)